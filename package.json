{
  "name": "my-memo",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --mode client && vite build",
    "preview": "$npm_execpath run build && vite preview",
    "deploy": "$npm_execpath run build && wrangler deploy",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:local:migrate": "wrangler d1 migrations apply my_memo_d1 --local",
    "db:local:reset": "rm -rf .wrangler/state/v3/d1",
    "db:remote:migrate": "wrangler d1 migrations apply my_memo_d1 --remote",
    "test": "pnpm run test:unit && pnpm run test:integration",
    "test:unit": "vitest run --config=vitest.unit.config.ts",
    "test:integration": "vitest run",
    "test:watch": "vitest --config=vitest.unit.config.ts",
    "test:browser": "vitest run --config=vitest.browser.config.ts",
    "test:collect": "node scripts/check-test-collection.mjs",
    "test:all": "pnpm run test:collect && pnpm run test && pnpm run test:browser",
    "ai-config": "mise run --no-cache sync-ai",
    "knip": "knip"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.9.0",
    "@phosphor-icons/core": "2.1.1",
    "@tailwindcss/vite": "^4.3.3",
    "better-auth": "^1.6.26",
    "daisyui": "^5.7.16",
    "drizzle-orm": "^0.45.2",
    "drizzle-zod": "^0.8.3",
    "hono": "^4.13.1",
    "honox": "^0.1.60",
    "markdown-it": "^15.0.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7",
    "@cloudflare/vitest-pool-workers": "0.21.0",
    "@hono/vite-build": "^1.11.1",
    "@hono/vite-dev-server": "^0.26.1",
    "@types/node": "^26.2.0",
    "@vitest/browser-playwright": "^4.1.10",
    "drizzle-kit": "^0.31.10",
    "knip": "^6.32.1",
    "tailwindcss": "^4.3.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.120.1"
  }
}
